# llm-graph-to-text-generation
Research project on the generation of natural language text from structured knowledge graphs using the WebNLG dataset. It features a dual-encoder architecture combining Graph Neural Networks (GNNs) and Large Language Models (LLMs) like LLaMA-2 to evaluate parameter-efficient fine-tuning (LoRA) and graph-aware text generation.  
